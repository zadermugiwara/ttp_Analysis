\documentclass[a4paper,11pt]{article}
\pdfoutput=1

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{siunitx}
\usepackage{booktabs}
\usepackage{jheppub} % for details on the use of the package, please see the JINST-author-manual
\usepackage{lineno}
\linenumbers
 \usepackage{float}
\graphicspath{{figs/}}
\sisetup{
  detect-all,
  table-number-alignment = center,
  table-align-text-post = false
}

\title{Agnostic search for a vector-like top partner at a lepton collider via the recoil mass technique}

\author{Higinio Valle,}
\author[1]{Cristina Oropeza Barrera\note{Corresponding author.}}
\author{and Oscar Ochoa-Valeriano}

\affiliation{Universidad Iberoamericana Ciudad de México, \\ Prolongación Paseo de la Reforma 880, Col. Lomas de Santa Fe, Álvaro Obregón, Mexico City 01219, Mexico}

\emailAdd{cristina.oropeza@ibero.mx}


\date{\today}

\abstract{A study of the single production of a vector-like top partner $T$ at a $\sqrt{s}=\SI{3}{TeV}$ $e^{+}e^{-}$ collider is presented, using a decay mode and mass agnostic event selection built around the recoil mass observable. The analysis relies on fully-simulated samples processed through a common reconstruction and event selection chain. The baseline selection is validated without taking into consideration beam polarization or initial-state radiation (ISR). The impact of more realistic running conditions, including ISR together with an $e^{-}$ beam polarization of $+80\%$, is then evaluated and the discovery reach is mapped as a function of the top partner mass $m_{T}$ and coupling strength $\kappa_{T}$. In the baseline scan, the estimated statistical significance rises above $3\sigma$ for the lowest benchmark masses once $\kappa_{T}$ exceeds approximately 0.3.}

\keywords{Vector-like quarks, Electron-positron collider, Recoil mass}

\begin{document}
\maketitle
\flushbottom
\section{Introduction}

Vector-like quarks (VLQs) are among the best-motivated extensions of the Standard Model (SM), particularly in scenarios that address the hierarchy problem \cite{hierarchy} through new dynamics in the top sector. They arise naturally in composite Higgs constructions~\cite{composite_vector_resonances, composite-Higgs_minimal, composite-Higgs_phenomenology}, little-Higgs realizations~\cite{little-Higgs}, and extra-dimensional frameworks~\cite{Antoniadis:1990}. In general, VLQs are introduced in 4 flavours $T$ (+2/3), $B$ (-1/3), $X$ (+5/3), $Y$ (-4/3) and couple to the third generation of SM quarks and electro-weak (EW) bosons \cite{VLQ_handbook}. The decay modes and branching fractions for these hypothetical particles depend on the chosen multiplet representation as well as on the peculiarities of the non-minimal models.
 
Current constraints on VLQs come from searches at the LHC \cite{CMS_review, ATLAS_review}, where large QCD rates are accompanied by substantial combinatorial and irreducible backgrounds. These searches are typically optimized for specific decay hypotheses, so their sensitivity can deteriorate rapidly away from the assumed branching-fraction pattern. A multi-TeV $e^{+}e^{-}$ collider offers a complementary and cleaner environment, particularly for single-production channels in which the sensitivity is directly affected by the size of the effective coupling $\kappa$. The proposed CLIC program \cite{brunner2022clicproject} at $\sqrt{s}=\SI{3}{TeV}$ is well suited to explore this regime. Recent phenomenological studies have focused on searches for single production of $T$ \cite{TCLICsearch} and $B$ \cite{BCLICsearch} in specific decay modes.

In this work, the process $e^{+}e^{-}\to T t$ is studied in a model setup following Ref.~\cite{Buchkremer:2013}. The analysis strategy is intentionally agnostic to both the top-partner mass $m_T$ and its decay mode. Unlike traditional LHC searches, which often rely on narrow resonance windows and channel-specific selections, the present approach exploits the known initial state of a lepton collider through the recoil mass against a reconstructed hadronic SM top quark candidate. This observable preserves sensitivity across a broad range of branching-fraction hypotheses without tailoring the selection to a single topology. Baseline sensitivities are first established using samples without initial-state radiation (ISR) or beam polarization in order to calibrate the intrinsic performance of the recoil-based selection. The impact of more realistic running conditions, including ISR together with $P_{e^-}=+80\%$, is then quantified. Finally, the expected significance in the $(m_T,\kappa_T)$ plane is mapped to provide a compact interpretation of the discovery reach.


\section{Signal and background samples}

This analysis uses the vector-like quark model developed by Buchkremer et al. \cite{Buchkremer:2013}. The top partner is introduced as a singlet that mixes exclusively with SM third generation quarks and decays to $qW$ (50\%), $qZ$ (25\%) and $qH$ (25\%).  
\begin{align*}
  \mathcal{L}_{\mathrm{eff}} = \kappa_T \Biggl\{
  & \sqrt{\frac{\zeta_i \xi_W^T}{\Gamma_W^0}}\,\frac{g}{\sqrt{2}}
    \left[\Bar{T}_{L/R} W_\mu^+ \gamma^{\mu} d_{L/R}^i\right] \\
  & + \sqrt{\frac{\zeta_i \xi_Z^T}{\Gamma_Z^0}}\,\frac{g}{2c_W}
    \left[\Bar{T}_{L/R} Z_\mu \gamma^{\mu} u_{L/R}^i\right] \\
  & - \sqrt{\frac{\zeta_i \xi_H^T}{\Gamma_H^0}}\,\frac{M}{v}
    \left[\Bar{T}_{R/L} H u_{L/R}^i\right]
  \Biggr\} + \text{h.c.}
\end{align*}

The signal process explored in this study, $e^{+}e^{-}\rightarrow T\,t$, is shown in Figure~\ref{fig:feynman}. Given the high masses of the top partners, the projected lepton collider that is the most suitable for this analysis is CLIC \cite{brunner2022clicproject} as it is the only one that could reach the necessary center-of-mass energies to produce such heavy particles. Therefore, the run conditions of CLIC-3 are used, including $\sqrt{s}=\SI{3}{TeV}$, electron-beam polarization, and a total expected integrated luminosity of $L=\SI{5}{ab^{-1}}$. To disentangle the intrinsic performance of the recoil-mass method from collider-specific effects, two signal configurations are considered throughout this study: an unpolarized sample without ISR and a more realistic scenario including ISR with $P_{e^-}=+80\%$.  

\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth, trim={1cm 21.5cm 2cm 1cm}, clip]{figs/feynman_diagram.png}
  \caption{Example Feynman diagram of the signal process.}
  \label{fig:feynman}
\end{figure}

Signal and SM background samples are generated at tree level with \textsc{MadGraph5\_\allowbreak aMC@NLO} \cite{Alwall:2014}, decayed with \textsc{MadSpin}~\cite{Artoisenet:2013}, and showered with \textsc{Pythia 8}~\cite{Sjostrand:2015}. The SM backgrounds considered in this study are $t\bar{t}$, $t\bar{t}Z$, $t\bar{t}H$, and $W^{+}W^{-}Z$. 

The analysis targets benchmark top partner masses $m_{T}$ between 1.2 and 2.4~TeV, in 400 GeV steps, with widths derived from the simulation campaign. Tables~\ref{tab:Tt1200Kappa}--\ref{tab:TtNoKappaGrouped} (see App.~\ref{app:widths}) summarize the $\kappa_{T}$-dependent widths extracted from the simulations.~Unless otherwise stated, the coupling strength is set to $\kappa_{T}=0.20$ for the baseline sensitivity studies, corresponding to a total width of \SI{22.3}{GeV} for $m_{T} = \SI{1.2}{TeV}$.


\section{Event selection and reconstruction}

The analysis strategy consists of reconstructing the hadronic decay of a SM top quark as a large radius (large-R) jet and calculating its recoil mass. 

The event selection demands at least one Cambridge--Aachen jet with radius $R=1.0$, $p_{T} > 100$~GeV and $|\eta|<5$. This large-$R$ jet must be tagged with the John Hopkins top-tagger~\cite{Kaplan:2008} and at least one of its subjets tagged as a b-jet.  

The recoil mass is computed event-by-event by solving
\begin{equation}
  m_{\text{recoil}}^{2} = s - 2\,E_{\text{top}}\sqrt{s} + m_{\text{top}}^{2},
  \label{eq:recoilMass}
\end{equation}
where $E_{\text{top}}$ and $m_{\text{top}}$ are reconstructed from the leading large-$R$ jet that passes the object selection requirements.  

Further selection criteria are applied to reduce the SM background contamination:
\begin{itemize}
  \item total missing energy in the center-of-mass frame, $E_{\text{miss}} < \SI{1}{TeV}$, which primarily suppresses the $W^{+}W^{-}Z$ and $t\bar{t}Z$ processes, and
  \item $m_{\text{recoil}} > 1.1$~TeV, to suppress the SM continuum.
\end{itemize}

These are the only requirements applied in order to maintain a decay-mode- and mass-agnostic analysis. As $m_{T}$ grows larger, the top partner becomes less boosted, so any additional requirements on the event kinematics would bias the reconstructed recoil-mass spectra.

The reconstructed recoil-mass distributions for the different signal samples are shown in Figure~\ref{fig:signal_after_cut}. A clear peak is observed around the nominal mass values for all samples. For the larger masses, a broad tail develops at the low end of the spectra. Generator-level studies indicate that this feature is associated with selecting the secondary top quark originating from the $T$ decay chain rather than the primary associated top quark produced in $e^{+}e^{-}\to Tt$.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.45\textwidth]{figs/signals_after_cuts.pdf}
  \caption{Recoil mass of the signal samples after the baseline selection cuts.}
  \label{fig:signal_after_cut}
\end{figure}


\section{Idealized baseline calibration}

Table~\ref{tab:baseline-cutflow} lists the weighted event yields for $m_{T}=\SI{1.2}{TeV}$ together with the dominant $t\bar{t}$ background after the main selection steps for the unpolarized, no-ISR samples.

\begin{table}[H]
  \centering
  \caption{Baseline cutflow yields for the $m_{T}=\SI{1.2}{TeV}$ signal with $\kappa_T = 0.20$ and for the $t\bar{t}$ background, scaled to $\SI{5}{ab^{-1}}$.}
  \label{tab:baseline-cutflow}
  \begin{tabular}{l S[table-format=5.1] S[table-format=6.1]}
    \toprule
    Selection step & {Signal} & {$t\bar{t}$} \\
    \midrule
    All events                & 1232.3 & 95706.0 \\
    Fat-jet preselection      & 1232.0 & 95335.1 \\
    Top tagged                & 401.4  & 41535.0 \\
    Substructure tag          & 348.6  & 37869.4 \\
    Isolation veto            & 215.1  & 31975.5 \\
    $m_{\text{recoil}}>\SI{1.1}{TeV}$ & 206.1  & 5897.1 \\
    $E_{\text{miss}}<\SI{1}{TeV}$     & 198.6  & 5654.6 \\
    \bottomrule
  \end{tabular}
\end{table}

Due to the potential presence of additional SM top quarks arising from the $T$ decay chain, an isolation requirement is applied to the large-$R$ jets, vetoing those with nearby activity either from high-$p_{T}$ leptons or small-radius jets. This requirement is imposed only on large-$R$ jets that have a relative HT\footnote{HT is defined as the scalar sum of the $p_{T}$ of all small-$R$ ($R=0.5$) Cambridge--Aachen jets in the event with $p_{T} > 20$~GeV. The relative HT is the fraction of the total HT carried by the selected large-$R$ jet.} below 0.4. Generator-level studies indicate that the primary associated top quark produced in $e^{+}e^{-}\to Tt$ typically recoils against the heavy top partner with a larger boost than the secondary top quark originating from the $T$ decay. As a consequence, the decay products of the primary top are more collimated and are more likely to be captured inside a single energetic large-$R$ jet that carries a sizable fraction of the event HT. By contrast, when the selected candidate corresponds to the secondary top quark from the $T$ decay, the event energy is shared with the accompanying boson and additional hadronic activity, so the chosen large-$R$ jet carries a smaller fraction of the total HT and is more likely to be accompanied by nearby small-$R$ jets or isolated leptons. The low-relative-HT region is therefore the one most sensitive to this misassignment, which explains why the isolation veto is most effective there. This isolation veto eliminates events containing, for example, isolated energetic leptons, thereby removing semileptonic SM top quark decays that would mimic the boosted-top signature while evading the recoil mass requirement.

After the missing-energy requirement, the $t\bar{t}$ sample is reduced to about \num{5.7e3} events while the signal retains \num{1.99e2} events.  Including the subleading electroweak contributions brings the total background expectation to \num{6.9e3} events.  The corresponding signal yields and counting significances, defined as $Z = S/\sqrt{S + B}$, are summarized in Table~\ref{tab:significance-summary} for each benchmark mass point.  Figure~\ref{fig:baseline-recoil} displays the recoil mass spectrum for the $m_{T}=\SI{1.2}{TeV}$ signal overlaid with the total SM background expectation.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.45\textwidth]{figs/stack_mrecoil_1200.pdf}
  \caption{Baseline recoil mass spectrum for $\kappa_T=0.20$ with no polarization or ISR.  Histograms are normalized to $\SI{5}{ab^{-1}}$.}
  \label{fig:baseline-recoil}
\end{figure}

\section{Realistic discovery reach}

To assess the discovery reach under more realistic running conditions, the analysis is repeated on samples that include ISR together with an electron-beam polarization of $+80\%$ (positrons remain unpolarized). Because the recoil-mass technique relies on the nominal center-of-mass energy, ISR broadens the reconstructed peak, while the change in beam polarization modifies both the signal rate and the background composition. Figure~\ref{fig:comparison-recoil} compares the total signal-plus-background expectation in the two configurations. The significance loss is summarized in Table~\ref{tab:significance-summary}; for $m_{T}=\SI{1.2}{TeV}$ the discovery reach drops from $Z=2.33$ to $Z=1.72$.

The observed loss in sensitivity also points to concrete mitigation handles for future analyses. ISR photons are emitted predominantly along the beam direction and therefore remove longitudinal energy while leaving the transverse structure of the event comparatively stable. This suggests that global observables such as the transverse-momentum imbalance of the reconstructed recoil system and the acolinearity of the top--recoil topology should provide additional discrimination against events with severe ISR. Incorporating these ISR-sensitive observables into a multivariate discriminant offers a physically motivated route to recover part of the lost significance while preserving the mass- and decay-agnostic character of the search.

\begin{figure}[H]
  \centering

  \begin{subfigure}{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{recoil_comparison_1200.pdf}
    \caption{$m_T=1200~\mathrm{GeV}$}
  \end{subfigure}\hfill
  \begin{subfigure}{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{recoil_comparison_1600.pdf}
    \caption{$m_T=1600~\mathrm{GeV}$}
  \end{subfigure}

  \vspace{0.5em}

  \begin{subfigure}[b]{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{recoil_comparison_2000.pdf}
    \caption{$m_T=2000~\mathrm{GeV}$}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{recoil_comparison_2400.pdf}
    \caption{$m_T=2400~\mathrm{GeV}$}
  \end{subfigure}

  \caption{Total signal-plus-background recoil-mass spectra at $\sqrt{s}=3~\mathrm{TeV}$ with $\mathcal{L}=5~\mathrm{ab^{-1}}$ for four $T$-mass benchmarks:
(a) $m_T=1200$ GeV, (b) $m_T=1600$ GeV, (c) $m_T=2000$ GeV, and (d) $m_T=2400$ GeV.
Each panel compares the baseline configuration to the ISR+$P_{e^-}$ scenario (see legend).}
  \label{fig:comparison-recoil}
\end{figure}

\begin{table}[H]
  \centering
  \caption{Signal yields ($S$) and counting significances ($Z$) at $L=\SI{5}{ab^{-1}}$ for the unpolarized baseline and the ISR+$P_{e^{-}}$ samples.}
  \label{tab:significance-summary}
  \begin{tabular}{c S[table-format=3.1] S[table-format=1.2] S[table-format=3.1] S[table-format=1.2]}
    \toprule
    $m_{T}$ [TeV] & {$S_{\text{base}}$} & {$Z_{\text{base}}$} & {$S_{\text{ISR+pol}}$} & {$Z_{\text{ISR+pol}}$} \\
    \midrule
    1.2 & 198.6 & 2.33 & 171.8 & 1.72 \\
    1.6 & 167.4 & 1.97 & 130.8 & 1.31 \\
    2.0 & 97.4  & 1.15 & 66.5  & 0.67 \\
    2.4 & 25.3  & 0.30 & 14.7  & 0.15 \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Scanning \texorpdfstring{$\kappa_T$}{kappa}}
To evaluate the sensitivity of the analysis strategy, the counting significance is estimated for different $\kappa_T$ values. The resulting map in the $(m_{T},\kappa_T)$ plane is visualized in Fig.~\ref{fig:kappa-scan-baseline}. The baseline configuration reaches $Z\simeq 5$ for $(m_{T},\kappa_T)=(\SI{1.2}{TeV},0.30)$, and the sensitivity falls below $Z=1$ once $m_{T}\gtrsim\SI{2}{TeV}$ unless $\kappa_T\gtrsim 0.4$. The sections of the $(m_T, \kappa_T)$ plane with no reported value (white) correspond to the configurations in which the simulations yielded unphysical results (e.g. extremely large widths).

These contours provide a transparent summary that can be reinterpreted for specific decay modes by rescaling the signal efficiencies.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/kappa_scan_baseline.png}
  \caption{Significance across the $(m_{T},\kappa_T)$ plane for the baseline configuration without ISR or beam polarization.}
  \label{fig:kappa-scan-baseline}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/kappa_scan_plus80ISR.png}
  \caption{Significance across the $(m_{T},\kappa_T)$ plane including ISR and $P_{e^{-}}=+80\%$.}
  \label{fig:kappa-scan-isr}
\end{figure}

\section{Discussion and outlook}
A prospective study of single production of a top partner $T$ at a $\sqrt{s}=\SI{3}{TeV}$ electron-positron collider has been presented using an analysis strategy that does not depend on the assumed mass or decay mode. The recoil-based selection remains robust against variations in the true top-partner mass and already yields $\mathcal{O}(2\sigma)$ sensitivity at $\kappa_T=0.20$ in the idealized baseline configuration. The comparison with the ISR+$P_{e^-}$ scenario then makes explicit how realistic collider effects reduce the reach by roughly $25\%$ in significance, while simultaneously identifying the global observables that should be prioritized to mitigate this loss.

In this sense, the present study does more than quantify a benchmark sensitivity. It outlines a concrete blueprint for future lepton-collider searches in the regions where channel-specific hadron-collider strategies deteriorate as branching-fraction assumptions fail. ISR-aware observables such as recoil-system transverse-momentum imbalance and top--recoil acolinearity, together with detector and reconstruction optimizations that improve the hadronic and substructure resolution of boosted large-$R$ jets, offer a direct path to strengthening the decay-agnostic recoil technique. More broadly, these results show that a mass- and decay-agnostic recoil strategy can become a foundational tool for probing vector-like top partners with minimal model prejudice across the $(m_T,\kappa_T)$ parameter space.

%Extending the scan to larger luminosities or combining with complementary decay channels will be crucial for excluding or discovering the full parameter space suggested by Figs.~\ref{fig:kappa-scan-baseline} and \ref{fig:kappa-scan-isr}.

\appendix
\section{Width tables}\label{app:widths}
\input{Tt_widths_table.tex}

\bibliographystyle{JHEP}
\bibliography{references}

\end{document}
